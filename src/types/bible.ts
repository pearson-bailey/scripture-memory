export type BotB = {
  id: number;
  name: string;
  abbrev: string;
  nlt?: string;
};

export type Verse = {
  reference: string;
  text: string;
  version: string;
};

export type Version = {
  id: number;
  name: string;
  abbrev: string;
};
